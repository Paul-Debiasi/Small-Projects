var divs = document.querySelector("div");
var bodies = document.querySelector("body");

bodies.addEventListener(mouseup, function (event) {
    if (mouseup === mouseup) {function getRandomColor() 
        return Math.floor(Math.random() * 101);
    
    }
});
